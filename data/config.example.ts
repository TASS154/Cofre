/**
 * EXEMPLO: Como configurar palavras destacadas para o Arquivo 1
 * 
 * Copie este arquivo para config.ts e preencha com as palavras reais
 * que devem aparecer destacadas em rosa no poema do Arquivo 1.
 * 
 * As palavras serão comparadas de forma case-insensitive e sem acentos.
 */

export const highlightedWords = {
  file1: [
    // Exemplo: se o poema contém "amor", "coração", "eterno"
    // e essas palavras formam a resposta, adicione-as aqui:
    // "amor",
    // "coração", 
    // "eterno"
  ],
}

